# Shift Booking API - Environment Configuration
# Copy this file to .env and configure for your environment

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Server port (default: 8080)
PORT=8080

# =============================================================================
# CORS CONFIGURATION
# =============================================================================

# Environment mode: 'production' or 'development'
# In production, ALLOWED_ORIGINS is required
NODE_ENV=development

# Comma-separated list of allowed origins for CORS
# Required in production mode, optional in development
# Examples:
#   Single origin:   ALLOWED_ORIGINS=https://your-app.example.com
#   Multiple origins: ALLOWED_ORIGINS=https://app.example.com,https://staging.example.com
#
# In development mode (NODE_ENV != 'production'), the following are automatically allowed:
#   - http://localhost:3000
#   - http://localhost:3001
#   - http://127.0.0.1:3000
#   - http://127.0.0.1:3001
#
# ALLOWED_ORIGINS=https://your-production-domain.com
